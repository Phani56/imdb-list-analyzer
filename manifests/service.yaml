kind: Service
apiVersion: v1
metadata:
  labels:
    app: imdb-list-analyzer
  name: imdb-list-analyzer
  namespace: imdb

spec:
  type: NodePort
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: imdb-list-analyzer
