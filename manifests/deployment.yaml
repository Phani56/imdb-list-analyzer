apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  namespace: workshop
  name: tomi-imdb-list-analyzer
  labels:
    app: tomi-imdb-list-analyzer

spec:
  progressDeadlineSeconds: 600
  replicas: 1
  selector:
    matchLabels:
      app: tomi-imdb-list-analyzer

  template:
    metadata:
      labels:
        app: tomi-imdb-list-analyzer
      annotations:
    spec:
      containers:
        - image: gcr.io/unity-ads-workshop-test/tkasu-imdb-list-analyzer:master
          name: tomi-imdb-list-analyzer
          imagePullPolicy: Always
          resources:
            requests:
              cpu: 0.1
              memory: 128Mi
            limits:
              cpu: 0.5
              memory: 1024Mi
          env:
            - name: SOME_ENV_VARIABLE
              value: some env value

      nodeSelector:
        cloud.google.com/gke-nodepool: workshop-pool