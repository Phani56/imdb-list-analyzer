apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  namespace: default
  name: imdbanatest
  labels:
    app: imdbanatest

spec:
  progressDeadlineSeconds: 600
  replicas: 1
  selector:
    matchLabels:
      app: imdbanatest

  template:
    metadata:
      labels:
        app: imdbanatest
      annotations:
    spec:
      containers:
        - image: gcr.io/imdbanatest/imdb-to-kube:latest
          name: imdbanatest
          imagePullPolicy: Always
          resources:
            requests:
              cpu: 0.1
              memory: 128Mi
            limits:
              cpu: 0.5
              memory: 1024Mi
          env:
            - name: SOME_ENV_VARIABLE
              value: some env value

      nodeSelector:
        cloud.google.com/gke-nodepool: default-pool